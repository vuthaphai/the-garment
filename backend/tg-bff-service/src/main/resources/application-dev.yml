app:
  data-adapter-url: ${DATA_ADAPTER_URL:http://localhost:8082}

jwt:
  secret: ${JWT_SECRET:thegarment-super-secret-key-must-be-at-least-32-chars}
  access-token-expiry: ${JWT_ACCESS_EXPIRY:3600000}
  refresh-token-expiry: ${JWT_REFRESH_EXPIRY:86400000}

logging:
  level:
    com.thegarment: DEBUG
