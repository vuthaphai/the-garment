app:
  data-adapter-url: ${DATA_ADAPTER_URL}

jwt:
  secret: ${JWT_SECRET}
  access-token-expiry: ${JWT_ACCESS_EXPIRY:3600000}
  refresh-token-expiry: ${JWT_REFRESH_EXPIRY:86400000}

logging:
  level:
    com.thegarment: WARN
    root: WARN
